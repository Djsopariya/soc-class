Unix command
pwd - prsent working directory 

Git command
l                     is long list
a                     shows hidden folder
.                     means current folder
..                    means parent folder
git init              initializes repository
git status            shows staus of the files that are not tracked
git add <filename>    adds file for tracking


git config --global user.email "you@example.com"  to set identity
git config --global user.name "Your Name"

$ git commit -m "Hello"  to commit filename
git log                  to see logs
git add .          




when u send code to servris called push
when u retrieve code from server its called pull
and whrn u first time copy code on new PC its called Clone


If u do not have pc then make edits in github from web and then pull data in Vs code
